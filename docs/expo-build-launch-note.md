# إعداد بناء APK المحدث

تاريخ التحقق: 14 أغسطس 2026.

تمّت مزامنة GitHub مع الالتزام `8175330b98d329bffc2786d76a61bd0fc0041dae`، والذي يعرّف الإصدار `1.0.1` ورقم بناء Android `2`. نموذج Expo متصل بالمستودع `alshahthy-cmyk/Linkora`. سيُنشأ البناء من المسار `artifacts/mobile`، لمنصة Android فقط، باستخدام ملف EAS المسمى `preview` وبيئة `Preview`، من دون إرسال تلقائي إلى المتجر.

يعرض النموذج تنبيهاً عن غياب بيانات اعتماد iOS. هذا التنبيه لا يخص بناء Android المحدد هنا ولا يستدعي أي إجراء في هذه المرحلة.
