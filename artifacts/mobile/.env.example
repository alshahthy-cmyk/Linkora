# Public build-time address for the deployed Express/WebSocket service.
# This value is included in the APK, so it must not contain secrets.
EXPO_PUBLIC_SIGNAL_URL=wss://signal.example.com/api/signal
